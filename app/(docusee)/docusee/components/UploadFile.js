import React from 'react';
// import './UploadFile.css'; // Create corresponding CSS or include styles as needed

const UploadFile = ({ }) => (
    <div className="upload-file">
        <p>업로드</p>
    </div>
);

export default UploadFile;